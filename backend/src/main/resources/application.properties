spring.application.name=stage-archive-backend
server.port=${PORT:8080}
stage.archive.directory=${STAGE_ARCHIVE_DIRECTORY:stage-archives}
stage.cors.allowed-origins=${STAGE_CORS_ALLOWED_ORIGINS:http://localhost:*,http://127.0.0.1:*}
